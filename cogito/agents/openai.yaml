interface:
  display_name: "Cogito"
  short_description: "Adopt, clarify, and execute software work"
  default_prompt: "Use $cogito to start one Cogito stage; direct answers can continue that stage without repeating $cogito."

policy:
  allow_implicit_invocation: true
