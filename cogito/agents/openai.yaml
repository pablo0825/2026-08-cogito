interface:
  display_name: "Cogito"
  short_description: "Gate-controlled multi-agent software delivery"
  default_prompt: "Use $cogito to clarify this work, prepare one Development Package for approval, and then follow the Gate through implementation, verification, independent review, integration, and finalization."

policy:
  allow_implicit_invocation: false
